# Phase 5 稳定性长测（24h）

- 开始：2026-09-04 09:59 +08
- **提前结束：2026-09-04 12:39 +08（约 2.7 小时，未满 24h）**
- 设备：`DIAL-0180B4`，STA `192.168.50.151`，SSID `Bituo-ext`
- Broker：本机 Mosquitto `192.168.50.32:1883`
- 采样：`tools/soak_poll.py` → `tools/captures/phase5_soak_20260904.jsonl`（约 5 分钟一条，全程 HTTP 200）

## 起点 `/api/info`（2026-09-04 09:59）

```json
{"dial_sn":"DIAL-0180B4","fw_ver":"1.1.0","ip":"192.168.50.151",
 "wifi":{"ssid":"Bituo-ext","rssi":-57,"connected":true},
 "mqtt":{"host":"192.168.50.32","port":1883,"connected":true},
 "ble_scan":{"running":true,"meter_count":1,"online_count":1},
 "uptime_s":397}
```

## 终点 `/api/info`（2026-09-04 12:39）

```json
{"wifi":{"connected":true},"mqtt":{"connected":true},
 "ble_scan":{"running":true,"meter_count":1,"online_count":1},
 "uptime_s":9997}
```

结论：**短时连续运行 PASS**（约 2.7h，uptime 单调上升未复位，Wi-Fi/MQTT/电表在线始终为真，采样无失败）。**不能代替任务书 11.5 的 24h。** 以后有整段时间可再跑 `python tools/soak_poll.py`。

MQTT 行偶发变黄再绿不单独判失败。本轮 HTTP 未带 heap 字段，内存泄漏未量化；无串口长 log，未直接看见 panic/wdt。

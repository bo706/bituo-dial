## IDF Component Manager 清单文件（ESP-IDF v5.2.3）
## 说明：从 ESP-IDF v5.2 起，mDNS 已移出 IDF 核心仓库，改为托管组件，
##       必须在此声明，CMake 配置阶段会自动从组件仓库下载。
## 版本约束 ~1.2.0：锁定 1.2.x（与 IDF v5.2.x 匹配；1.3+ 面向更新版本）。
dependencies:
  espressif/mdns: "~1.2.0"
